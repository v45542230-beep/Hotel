@model IEnumerable<Hotel.Models.Booking>
@{
    ViewData["Title"] = "Брони";
}
<h2>Брони</h2>
<form asp-controller="Home" asp-action="CheckBooking" method="Post" id="search" class="d-inline">
    <input type="text" name="column" />
    <select name="field" class="filter">
        <option value="id_r">номер</option>
        <option value="begin_date">дата въезда</option>
        <option value="end_date">дата выезда</option>
    </select>
    <input type="submit" value="Найти" class="btn btn-success w-30 p-1" />
</form>
<div id="testTableContainer">@Html.Partial("_CheckBooking", Model)</div>
<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
<script>
    $(document).ready(function () {
        $('#search').submit(function (e) {
            e.preventDefault();
            $.ajax({
                url: '@Url.Action("CheckBooking", "Home")',
                type: 'POST',
                data: $(this).serialize(),
                success: function (data) {
                    $('#testTableContainer').html(data);
                },
                error: function (xhr, status, error) {
                    console.error(error);
                }
            });
        });
    });
</script>